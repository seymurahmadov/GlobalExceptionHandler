spring:
  datasource:
    username: postgres
    url: jdbc:postgresql://postgres:5432/docker_postgres
    password: 12345
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      show-sql: 'true'
      ddl-auto: update


